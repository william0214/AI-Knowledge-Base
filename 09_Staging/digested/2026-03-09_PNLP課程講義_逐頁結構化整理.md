---
title: "PNLP 課程講義逐頁結構化整理"
date: 2026-03-13
source_type: pdf
status: digested
source_pdf: "../01_Courses/Python中文NLP與LLM專家課程/assets/PNLP上課筆記_2026-03-09.pdf"
---

# PNLP 課程講義逐頁結構化整理

> 原始檔案：[PNLP上課筆記_2026-03-09.pdf](../01_Courses/Python中文NLP與LLM專家課程/assets/PNLP上課筆記_2026-03-09.pdf)
> 說明：此檔依據 PDF 逐頁整理為結構化 Markdown。每頁保留頁碼，並盡量保留章節層次、步驟、輸入輸出、表格與圖表語意。若原始轉寫未保留圖像細節，則明確標示。

## 導入與環境準備

### Page 1 — Welcome and Hello!
#### 主題
- 課程介紹與環境準備起始。
#### 重點
- 講師為 David Lanz，聯絡信箱是 `DavidLanz@gmail.com`。
- Anaconda 與 Google Chrome 已由恆逸預先安裝。
- 課堂中不要升級或修補任何 Python 套件。
#### 步驟 / 輸入輸出
1. 從 USB 複製 `C:\!!David\Anaconda\Anaconda3.rar`。
2. 下載環境：`https://bit.ly/2BDyzFs`，解壓縮到 `C:\Programming\`。
3. 將 Gmail 帳號寄給講師並接受共享 Google Drive 邀請。
4. 共享資料夾：`https://drive.google.com/drive/folders/1R7Wl4fhd91e07RbV9tFspZcPcVuby1Jt?usp=sharing`。
#### 圖表 / 表格描述
- 可依步驟還原為一條標準 RAG 流程：`資料夾中的文件` -> `文件切割` -> `多語 Embedding` -> `向量索引 / 向量庫`。
- 查詢時的箭頭方向為：`使用者問題` -> `Retriever` -> `取回相關 chunks` -> `Gemini-Pro` -> `答案輸出`。
- 若原圖為架構圖，核心節點應是「文件來源、切塊器、Embedding、Retriever、LLM」，重點在資料先索引、後檢索、再生成。

### Page 2 — 課程目標
#### 主題
- 完整課程階段與 Lesson 架構。
#### 重點
- 第一階段：Python 爬蟲、文本分類、NLP 演算法、情感分析、社群聆聽。
- 第二階段：Transformer、RAG、Assistants API、AI Agents、開源模型微調、Neo4j 知識推理、GGUF、離線部署。
- Bonus：n8n 與 AI Agent 應用。
#### 步驟 / 輸入輸出
- 第一階段（7 小時）：Lesson 3, 4, 8, 9, 10, 11, 12, 15, 19。
- 第二階段（21 小時）：Lesson 20, 21, 24, 25, 25.4, 27–35, 40, 43, 45, 46.2, 50.x, 51, 55, 56, 57.1, 58。
#### 圖表 / 表格描述
- 這一頁本質上是課程總表，可視為課程模組清單表。

### Page 3 — 本堂課環境的安裝
#### 主題
- Anaconda 下載與安裝。
#### 重點
- 安裝時選擇略過註冊（Skip registration）。
#### 步驟 / 輸入輸出
1. 造訪 `https://www.anaconda.com/products/individual`。
2. 下載並執行安裝程式。
3. 安裝過程中略過註冊。
#### 圖表 / 表格描述
- 可還原為一個服務建置流程圖：`建立 conda 環境 uuugpt` -> `安裝 Flask / LangChain / PDF / Embedding 依賴` -> `建立 RAG API`。
- 若圖中含模組框，應可拆為三層：`文件處理層`、`向量檢索層`、`Flask API 封裝層`。
- 對外資料流方向為：`HTTP Request` -> `Flask app` -> `Retriever / LLM` -> `JSON Response`。

### Page 4 — 設定環境變數
#### 主題
- Windows 環境變數設定。
#### 重點
- 需將 Python/Anaconda 路徑加入系統環境變數。
#### 步驟 / 輸入輸出
- 本頁轉寫僅保留標題，實際操作畫面應以原始 PDF 為準。
#### 圖表 / 表格描述
- 原始 PDF 應為操作畫面截圖，但轉寫未保留節點與箭頭細節。

### Page 5 — 選擇剛才解壓縮的路徑 D:\Anaconda3
#### 主題
- 指定 Anaconda 安裝或解壓縮路徑。
#### 重點
- 路徑固定為 `D:\Anaconda3`。
#### 步驟 / 輸入輸出
1. 選擇解壓縮路徑 `D:\Anaconda3`。
2. 點選「確定」。
#### 圖表 / 表格描述
- 原始 PDF 為 UI 操作畫面，轉寫未保留可描述的流程圖細節。

### Page 6 — 補充環境變數路徑
#### 主題
- 完整設定 Anaconda 三個環境變數目錄。
#### 重點
- 必須新增三個路徑：`D:\Anaconda3`、`D:\Anaconda3\Scripts`、`D:\Anaconda3\Library\bin`。
#### 步驟 / 輸入輸出
- 輸入：將上述三個資料夾加入 PATH。
- 輸出：之後可直接使用 `python`、`conda`、相關 DLL。
#### 圖表 / 表格描述
- 本頁可視為路徑清單。

### Page 7 — Environment check
#### 主題
- 驗證 Python 環境是否安裝成功。
#### 重點
- 使用版本檢查命令驗證安裝。
#### 步驟 / 輸入輸出
- 輸入：`C:\>python --version`
- 輸出：顯示已安裝 Python 版本。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 8 — 建立 Anaconda 虛擬環境
#### 主題
- 建立 `uuunlp` 虛擬環境並安裝課程套件。
#### 重點
- Python 版本為 3.7。
- 需要下載 `requirements.txt`，解壓縮密碼 `uuu`。
- 使用 `conda init` 初始化後，重新開啟 CMD 再 `activate`。
#### 步驟 / 輸入輸出
1. `cd C:\Programming`
2. 下載 requirements 壓縮檔並解壓到 `C:\Programming`。
3. 建立環境：`conda create -n uuunlp python=3.7`
4. 初始化：`conda init`
5. 重開 CMD 後：`conda activate uuunlp`
6. 安裝套件：
```bash
pip install ipykernel
python -m ipykernel install --user --name uuunlp --display-name uuunlp
pip install ipywidgets widgetsnbextension pandas-profiling
pip install notebook==6.5.7
pip install -r requirements.txt
pip install -U ckiptagger
pip install -U transformers
pip install -U ckip-transformers
pip install plac
pip install tabulate
pip install --upgrade nbformat
pip install --upgrade accelerate
pip install protobuf==3.20
pip install h5py==2.10.0
pip install Delorean
```
7. 啟動 Jupyter：
```bash
C:\Anaconda3\Scripts\jupyter.exe notebook --ip="127.0.0.1" --no-browser --port=9487
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 9 — 在 `(uuunlp) C:\Programming>` 安裝套件
#### 主題
- 套件安裝命令與 Jupyter 設定。
#### 重點
- 所有安裝命令都在 `uuunlp` 環境內執行。
- `requirements.txt` 應放在 `C:\Programming`。
#### 步驟 / 輸入輸出
- 與 Page 8 相同的安裝命令清單。
- Jupyter 啟動命令：`jupyter.exe notebook --ip="127.0.0.1" --no-browser --port=9487`。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 10 — 移除虛擬環境的指令
#### 主題
- `uuunlp` 環境停用、刪除與 Jupyter kernel 清理。
#### 重點
- 明確註記：課堂中不要執行。
#### 步驟 / 輸入輸出
```bash
(uuunlp) C:\>conda deactivate
C:\>conda remove --name uuunlp --all
C:\>jupyter kernelspec uninstall uuunlp
C:\>conda env list
```
- 最後手動刪除 `C:\Anaconda3\envs\uuunlp`。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 11 — Environment preparation
#### 主題
- 課程範例環境檔下載與配置。
#### 重點
- 下載 `ExampleEnv.rar`，解壓密碼為 `uuu`。
- 解壓縮到 `C:\Programming\`。
#### 步驟 / 輸入輸出
1. 下載：`https://www.dropbox.com/s/vbztpjl98kcceuy/ExampleEnv.rar?dl=0`
2. 解壓縮至 `C:\Programming\`。
3. 取得 `ExampleEnv` 資料夾結構。
#### 圖表 / 表格描述
- 原始 PDF 應顯示資料夾結構圖，但轉寫未保留細節。

### Page 12 — Download Source code and put them under Environment folder
#### 主題
- 課堂範例程式碼放置位置。
#### 重點
- 每個 lesson 的 source code 由講師課堂中提供。
- 需放在 Environment folder 下。
#### 步驟 / 輸入輸出
- 輸入：講師提供的壓縮檔或程式碼。
- 輸出：對應 Lesson 的範例資料夾與檔案。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 13 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無實質教學內容。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 14 — Lesson 3 - Crawler & Fetch training data
#### 主題
- 網頁爬蟲與訓練資料集建立。
#### 重點
- 涵蓋 `urllib.request`、`requests`、`BeautifulSoup`、`Selenium`。
- 可由 `ld+json` 建立訓練資料。
- 包含 Google Play APP package crawler。
#### 步驟 / 輸入輸出
- Source code：`https://www.dropbox.com/s/z7lkhxb3xpq8qwn/lesson03.rar?dl=0`
- 啟動 notebook：
```bash
cd Programming\ExampleEnv
conda activate uuunlp
(uuunlp) C:\Programming\ExampleEnv>jupyter notebook
```
- 下載相容版 Chromedriver：`https://googlechromelabs.github.io/chrome-for-testing/#stable`
- 解壓縮至 `C:\Programming\ExampleEnv\Assets\`。
- 安裝 Sublime Pretty JSON 套件。
#### 圖表 / 表格描述
- 本頁應有 ChromeDriver / Sublime 操作畫面，但轉寫未保留流程箭頭細節。

### Page 15 — Sublime Text 4 Key Bindings Configuration
#### 主題
- Pretty JSON 快捷鍵與 Chromedriver 更新流程。
#### 重點
- `ctrl+alt+j` 對應 `pretty_json`。
- `ctrl+alt+m` 對應 `un_pretty_json`。
- `ctrl+r` 對應 JSON symbol 跳轉。
#### 步驟 / 輸入輸出
```json
[
  {"keys": ["ctrl+alt+j"], "command": "pretty_json"},
  {"keys": ["ctrl+alt+m"], "command": "un_pretty_json"},
  {
    "keys": ["ctrl+r"],
    "command": "pretty_json_goto_symbol",
    "context": [{"key": "selector", "operator": "equal", "operand": "source.json"}]
  }
]
```
- Chrome 升級後需下載對應版本 Chromedriver。
#### 圖表 / 表格描述
- 本頁可視為設定表；原始圖像未保留。

## 中文 NLP 基礎、分詞、情感與分類

### Page 16 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 17 — Lesson 8 - Text pre processing, jieba, TF-IDF
#### 主題
- 中文文本前處理、jieba 分詞、TF-IDF 與 Bayes。
#### 重點
- 涵蓋 Bayes Classification、jieba 中文斷詞、Bag of Words、N-gram、TF-IDF。
- 可用 Bayes 做中文分類器。
#### 步驟 / 輸入輸出
- 輸入：中文文本。
- 輸出：分詞結果、TF-IDF 特徵、分類結果。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 18 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 19 — CKIP Transformers 範例程式
#### 主題
- CKIP Transformers 的分詞、詞性標註、NER 實作。
#### 重點
- 使用 `CkipWordSegmenter`、`CkipPosTagger`、`CkipNerChunker`。
- 可用 CPU（`device=-1`）或 GPU（`device=0`）。
- 支援繁體中文。
#### 步驟 / 輸入輸出
- 輸入：中文句子。
- 輸出：分詞、POS、NER。
```python
import time
from ckip_transformers.nlp import CkipWordSegmenter, CkipPosTagger, CkipNerChunker
ws_driver = CkipWordSegmenter(device=-1)
pos_driver = CkipPosTagger(device=-1)
ner_driver = CkipNerChunker(device=-1)
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 20 — CKIP 實體抽取函式續接
#### 主題
- 抽取實體後的結果整理與去重。
#### 重點
- NER 返回 `(text, label, idx)`。
- 後續可搭配分詞與詞性過濾有效詞。
#### 步驟 / 輸入輸出
- 範例輸入：包含人物、地點與商品的中文句子。
- 輸出：分類後的實體字典與詞語列表。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 21 — jieba 可接受的詞性
#### 主題
- jieba 詞性標籤系統。
#### 重點
- 包含 `adj`、`adv`、`conj`、`int`、`m`、`n`、`o`、`prep`、`pron`、`punc`、`q`、`u`、`unknown`、`v` 等。
#### 步驟 / 輸入輸出
- 輸入：jieba 分詞結果。
- 輸出：詞性標籤，可用於過濾與特徵抽取。
#### 圖表 / 表格描述
| 詞性 | 說明 |
|---|---|
| adj | 形容詞 |
| adv | 副詞 |
| conj | 連接詞 |
| n | 名詞 |
| v | 動詞 |
| punc | 標點符號 |

### Page 22 — 中研院斷詞系統與 CKIP 實體標籤
#### 主題
- CKIP 系統與 NER 實體類型說明。
#### 重點
- 官方網站：`https://ckip.iis.sinica.edu.tw/service/transformers/`
- GitHub：`https://github.com/ckiplab/ckip-transformers`
- 支援多種實體類型：PERSON、ORG、GPE、PRODUCT、EVENT、LAW、LANGUAGE、DATE、TIME、PERCENT、MONEY、QUANTITY、ORDINAL、CARDINAL 等。
#### 步驟 / 輸入輸出
- 輸入：繁體中文文本。
- 輸出：斷詞、詞性與命名實體標籤。
#### 圖表 / 表格描述
| 類型 | 說明 |
|---|---|
| PERSON | 人物 |
| ORG | 組織 |
| GPE | 地緣政治實體 |
| PRODUCT | 產品 |
| DATE | 日期 |
| MONEY | 金額 |

### Page 23 — @CKIP 斷詞時間測試
#### 主題
- CKIP 在 CPU / GPU 上的效能比較。
#### 重點
- CPU 測試平台為 5900X。
- GPU 測試平台為 3090。
#### 步驟 / 輸入輸出
- CPU：1 thread 140s、2 thread 72s、4 thread 41s、8 thread 31s、24 thread 27s。
- GPU：3090 跑 1000 篇耗時 24 秒。
#### 圖表 / 表格描述
| 平台 | 規模 | 時間 |
|---|---|---|
| CPU 1 thread | 100 篇 | 140s |
| CPU 24 threads | 100 篇 | 27s |
| GPU 3090 | 1000 篇 | 24s |

### Page 24 — Lesson 10 - Text Analytics & Word2Vec
#### 主題
- 文本分析與 Word2Vec 詞向量。
#### 重點
- 電影劇本 Scripts analytics。
- 用 Word2Vec 挖掘語義相似關係。
- 從資料夾載入文本訓練模型。
- 分析人物關聯與美容類文本。
#### 步驟 / 輸入輸出
- 輸入：文本語料。
- 輸出：詞向量模型與相似詞查詢結果。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 25 — Lesson 11 - Sentiment Analysis
#### 主題
- 情感分析方法總覽。
#### 重點
- SnowNLP。
- AFINN 行銷模型。
- Naive Bayes、XGBoost。
- RNN 情感評分。
- Google Cloud Natural Language。
#### 步驟 / 輸入輸出
- 資料集：Google Play 評論。
- 輸出：情感標籤與信心分數。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 26 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 27 — Google Language API
#### 主題
- Google Cloud Natural Language API 與環境建立。
#### 重點
- 需要 GCP 帳號與付款方式。
- 建立 `uuugoogle` 虛擬環境，Python 3.10。
#### 步驟 / 輸入輸出
```bash
conda create -n uuugoogle python=3.10
conda activate uuugoogle
pip install ipykernel
python -m ipykernel install --user --name uuugoogle --display-name uuugoogle
pip install ipywidgets widgetsnbextension pandas-profiling
pip install --upgrade google-cloud-language
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 28 — Lesson 21 - Google Play BERT Sentiment Classifier
#### 主題
- 用 BERT 進行情感分類。
#### 重點
- 包含資料載入、訓練/驗證切分、max_len、Dataset、Dataloader、模型選擇與測試。
#### 步驟 / 輸入輸出
- 輸入：Google Play 評論資料。
- 輸出：情感分類模型與測試表現。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 29 — Lesson 22 - Fine-tuning RNN
#### 主題
- RNN 文本分類與遷移式學習。
#### 重點
- 邏輯迴歸、Naive Bayes、SVM、RNN、K-Fold。
- 儲存模型與微調模型。
#### 步驟 / 輸入輸出
- 輸入：文本資料集。
- 輸出：已訓練分類器與微調後模型。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 30 — Multi-Class, Multi-Label 多類別文章分類與標籤文章分類
#### 主題
- 多分類與多標籤分類概念對照。
#### 重點
- Multi-Class：一筆資料只有一個標籤。
- Multi-Label：一筆資料可同時有多個標籤。
#### 步驟 / 輸入輸出
- 多分類例子：垃圾郵件、情感三分類、新聞主題分類。
- 多標籤例子：電影同時屬於動作與喜劇、新聞同時屬於娛樂與運動。
#### 圖表 / 表格描述
| 類型 | 規則 | 例子 |
|---|---|---|
| Multi-Class | 一筆資料一個標籤 | 男/女、正/中/負 |
| Multi-Label | 一筆資料多個標籤 | 娛樂 + 運動 |

### Page 31 — Lesson 9 - Text Classification
#### 主題
- 中文文本分類方法與實作流程。
#### 重點
- 使用 jieba + TF-IDF。
- SVM baseline。
- Naive Bayes 與 XGBoost。
- 支援繁中、簡中與英文。
#### 步驟 / 輸入輸出
1. TF-IDF + SVM。
2. 餘弦相似度做相似句機率。
3. Naive Bayes 小資料分類。
4. 停用詞存 pickle。
5. 大資料三分類：3C、美妝、美食。
6. XGBoost 與 Multi-Class。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 32 — Lesson 12 - Gender Prediction Based on Name
#### 主題
- 英文與中文姓名性別預測。
#### 重點
- 對應兩類資料：英文名與中文名。
#### 步驟 / 輸入輸出
- 輸入：姓名。
- 輸出：性別預測結果。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 33 — Lesson 15 - Update Jieba or Ckip Chinese dictionary
#### 主題
- 用 N-Gram 發掘新關鍵詞並更新字典。
#### 重點
- Unigram、Bigram、Trigram。
#### 步驟 / 輸入輸出
- 依 N-Gram 統計詞彙出現特徵，用於擴充 jieba 或 CKIP 字典。
#### 圖表 / 表格描述
| n | 名稱 |
|---|---|
| 1 | Unigram |
| 2 | Bigram |
| 3 | Trigram |

### Page 34 — Day break;
#### 主題
- 日間休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 35 — Morning!
#### 主題
- 課程複習與提問。
#### 重點
- 監督/非監督學習差異。
- pair plot。
- WordNet、one-hot、Word2Vec 的應用。
- TF-IDF 原理。
- Hugging Face 帳號。
- 多標籤資料集準備。
- HTML 關鍵詞提取與 Jieba 字典。
- Jieba vs CKIP Transformers。
#### 步驟 / 輸入輸出
- 需從講師 USB 複製 Gemma 與 embedding 模型，不直接下載。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 36 — Lesson 21 - Google Play BERT Sentiment Classifier
#### 主題
- Google Play 評論的 BERT 情感分類。
#### 重點
- 匯入套件、資料集、訓練資料、max_len、Dataset、Dataloader、模型選擇、自建 BERT 模型、測試。
#### 步驟 / 輸入輸出
- Google Colab：`https://colab.research.google.com/drive/1OTcTZ6Yctj73oSZUf9Wq0q_RiJTGQHWJ?usp=sharing`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 37 — Lesson 22 - Fine-tuning RNN
#### 主題
- RNN 微調流程與基準模型比較。
#### 重點
- 匯入套件、載入資料集、資料處理、邏輯迴歸、Naive Bayes、SVM、RNN、K-Fold、模型保存與微調。
#### 步驟 / 輸入輸出
- Google Colab：`https://colab.research.google.com/drive/1xTWLk_M56fCvR71D8QxDCUFRIyU7k8eH?usp=sharing`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 38 — LAB: 大衛的中文情感模型
#### 主題
- 載入講師在 HuggingFace 發布的中文情感模型。
#### 重點
- 模型：`DavidLanz/fine_tune_chinese_sentiment`
- 五分類：Semi-negation、Negation、Neutral、Semi-positive、Positive。
#### 步驟 / 輸入輸出
```python
import torch
from transformers import BertForSequenceClassification, BertTokenizer

tokenizer = BertTokenizer.from_pretrained('DavidLanz/fine_tune_chinese_sentiment')
model = BertForSequenceClassification.from_pretrained('DavidLanz/fine_tune_chinese_sentiment')
```
- 實作 `predict_sentiment()` 後，輸入句子得到前 5 類情感分數。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 39 — 情感模型結果排序
#### 主題
- 情感預測結果後處理與排序。
#### 重點
- 依置信度排序情感標籤。
#### 步驟 / 輸入輸出
```python
sorted_data = sorted(ps, key=lambda x: list(x.values())[0], reverse=True)
highest_key = list(sorted_data[0].keys())[0]
highest_value = list(sorted_data[0].values())[0]
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 40 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 41 — 註冊 ChatGPT / Gemini / Claude / Grok
#### 主題
- 生成式 AI 平台帳號準備。
#### 重點
- 需註冊 ChatGPT、Google Gemini、Claude、Grok。
#### 步驟 / 輸入輸出
- 依序造訪：`https://chatgpt.com/`、`https://gemini.google.com/`、`https://claude.ai/`、`https://grok.com/`。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 42 — HuggingFace
#### 主題
- HuggingFace 帳號與 Access Token 設定。
#### 重點
- Follow 講師：`https://huggingface.co/DavidLanz`
- 建立 Write 權限的 Access Token。
#### 步驟 / 輸入輸出
1. 登入 HuggingFace。
2. 大頭照 -> Access Token -> New token。
3. Token 類型選 Write。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 43 — 建立四個 HuggingFace 模型倉庫
#### 主題
- 事先建立課堂需要的模型 repository。
#### 重點
- `tcp2023`
- `uuu_fine_tune_taipower`
- `uuu_fine_tune_gpt2`
- `llama2_uuu_news_qlora`
#### 步驟 / 輸入輸出
- 建立四個模型，授權建議選 Apache 2.0。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 44 — Apache 2.0 / MIT / GPL 授權比較
#### 主題
- HuggingFace 模型授權選擇。
#### 重點
- Apache 2.0：可自由使用、修改、分發，但保留原作者聲明。
- MIT：更寬鬆，不要求保留版權聲明。
- GPL：修改版需以 GPL 再發布。
#### 步驟 / 輸入輸出
- 在建立模型時選定適用授權。
#### 圖表 / 表格描述
| 授權 | 特性 |
|---|---|
| Apache 2.0 | 常用，需保留版權聲明 |
| MIT | 更寬鬆 |
| GPL | 修改版需 GPL |

### Page 45 — Lesson 20 - Fine-Tune GPT-2 for Text Generation
#### 主題
- 使用 GPT-2 做文本生成微調。
#### 重點
- 從 Excel 載入資料。
- 建立 TextDataset。
- 載入 GPT-2 與 tokenizer。
- TrainingArguments、Trainer、訓練、測試與上傳 HuggingFace。
#### 步驟 / 輸入輸出
- Google Colab：`https://colab.research.google.com/drive/1USNGRzSe21fQAVzSMtNV1IvV05pY2Lq-?usp=sharing`
- 課後要中斷並刪除 Colab 執行階段，歸還 GPU。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

## GPU、RAG 與部署

### Page 46 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 47 — Google Colab T4 的使用限制
#### 主題
- Colab 免費版與 Pro 的 GPU 使用限制。
#### 重點
- 免費版會隨使用量增加而縮短可執行時間、增加斷線頻率。
- Google 可能因過度使用而限制帳戶，但不一定說明原因。
#### 步驟 / 輸入輸出
- 常見錯誤：無法連線至 GPU backend。
- 解法：Pay As You Go 或訂閱 Colab Pro。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 48 — Colab Pro 與 GPU 時間限制
#### 主題
- 24 小時上限與節省 GPU 使用的方法。
#### 重點
- 每個工作階段最長 24 小時 GPU 時間。
- 可減少 batch size、縮短 epoch、非必要時關閉 GPU。
#### 步驟 / 輸入輸出
- 若需長時間執行，可改買 GCP VM 或 TPU。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 49 — 免費的 Kaggle 29GB RAM / P100
#### 主題
- Kaggle 免費資源概覽。
#### 重點
- 提供 29GB RAM、4 CPU、1 GPU（15.9GB VRAM）。
- 每週 30 小時限制。
#### 步驟 / 輸入輸出
- 官方：`https://www.kaggle.com/`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 50 — Windows 本地 PyTorch GPU 環境
#### 主題
- CUDA 12.4 + cuDNN + PyTorch 2.6.0 在 Windows 11 的安裝。
#### 重點
- 下載 CUDA SDK、cuDNN，解壓到 NVIDIA CUDA 目錄。
#### 步驟 / 輸入輸出
1. CUDA：`https://developer.nvidia.com/cuda-12-4-0-download-archive`
2. cuDNN：`https://developer.nvidia.com/rdp/cudnn-archive`
3. 解壓至 `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.4`
4. 安裝：
```bash
pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
```
5. 測試：`torch.cuda.is_available()`。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 51 — Ubuntu CUDA 環境安裝（CUDA 12.4）
#### 主題
- Ubuntu 22.04 下的 NVIDIA 驅動、CUDA、cuDNN、PyTorch 全流程。
#### 重點
- 建議用 `.deb` 安裝 CUDA，不用 `.run`。
- 環境變數加入 `~/.bashrc`。
#### 步驟 / 輸入輸出
```bash
sudo add-apt-repository ppa:graphics-drivers/ppa
sudo apt update
sudo ubuntu-drivers autoinstall
sudo reboot
wget https://developer.download.nvidia.com/compute/cuda/12.4.1/local_installers/cuda-repo-ubuntu2204-12-4-local_12.4.1-1_amd64.deb
sudo dpkg -i cuda-repo-ubuntu2204-12-4-local_12.4.1-1_amd64.deb
sudo apt-get update
sudo apt-get -y install cuda
```
- 建立 conda 環境並安裝 PyTorch。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 52 — GPU 可用性測試
#### 主題
- 驗證 GPU 是否可被 PyTorch 使用。
#### 重點
- 驗證可用性與顯示卡名稱。
#### 步驟 / 輸入輸出
```python
import torch
print(torch.cuda.is_available())
print(torch.cuda.get_device_name(0))
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 53 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 54 — Lesson 29 - Retrieval-Augmented Generation with OpenAI
#### 主題
- 使用 OpenAI 與 LangChain 建立 RAG。
#### 重點
- 安裝套件、下載資料集、設定 OpenAI API Key。
- 切分資料、向量化、存到 Chroma。
- SelfQueryRetriever、LlamaIndex VectorIndexRetriever、Chat Engine。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1qH68SGn3836IgxU4at1TiFDsSi6XUBO_?usp=sharing`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 55 — Day break;
#### 主題
- 日間休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 56 — Lesson 33 - Google Gemini-Pro + Langchain + RAG
#### 主題
- Gemini Pro 結合 LangChain 與 Embedding 做 RAG。
#### 重點
- 探索 Gemini-Pro、文件切割、多語系 Embedding、整個資料夾做 RAG。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1in7LnXGQktjeXaz6_KoSx76nrP7tmNBY?usp=sharing`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 57 — Lesson 34 - Building Flask APIs for Retrieval Augmented Generation (RAG)
#### 主題
- 建立 `uuugpt` 環境與 Flask RAG 服務。
#### 重點
- `python=3.10`。
- 安裝 Flask、LangChain、pypdf、tiktoken、google-generativeai、faiss-cpu 等。
#### 步驟 / 輸入輸出
```bash
conda create -n uuugpt python=3.10
conda activate uuugpt
pip install ipykernel
python -m ipykernel install --user --name uuugpt --display-name uuugpt
pip install Flask Flask-Uploads openai pinecone-client
pip install langchain langchain-community pypdf==4.3.1 tiktoken
pip install google-generativeai langchain-google-genai
pip install langchain_huggingface
pip install --upgrade "unstructured[pdf]==0.15.1"
pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1
pip install openai -U
pip install langchain_openai flask flask-cors -U
pip install faiss-cpu
pip install "transformers<4.53.0" "sentence-transformers<4.0.0" "accelerate<1.0.0"
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 58 — Local RAG Server UUUGPT 下載與設定
#### 主題
- 下載講師提供的 UUUGPT.zip 並設定 API key。
#### 重點
- 解壓密碼為 `uuu`。
- 解壓後會建立 `C:\Programming\ExampleEnv\UUUGPT\`。
- 在 `config.py` 填入 Google Gemini API key。
#### 步驟 / 輸入輸出
- 下載：`https://drive.google.com/file/d/1MqYGtcHYkwjpmDmhj5eEt2QxRuYZfrPS/view?usp=sharing`
#### 圖表 / 表格描述
- 可從操作步驟重建為設定流程：`下載 UUUGPT.zip` -> `解壓到 C:\Programming\ExampleEnv\UUUGPT\` -> `打開 config.py` -> `填入 Gemini API key`。
- 若圖中有資料夾樹，主要節點應為 `UUUGPT 根目錄`、`API / app.py`、`config.py` 等設定檔。
- 箭頭關係應強調「先下載與解壓，再修改設定，最後才能啟動服務」。

### Page 59 — 啟動 API Server
#### 主題
- 啟動 UUUGPT 的 API 服務。
#### 重點
- 在 API 資料夾下執行 `python app.py`。
#### 步驟 / 輸入輸出
```bash
cd C:\Programming\ExampleEnv\UUUGPT\API
python app.py
```
- 輸出：Server 啟動畫面。
#### 圖表 / 表格描述
- 原始 PDF 應有 Server 執行截圖，但轉寫未保留細節。

### Page 60 — 另開命令視窗與上傳檔案
#### 主題
- 切換至 UUUGPT 根目錄並執行檔案上傳。
#### 重點
- 下一步會執行 `upload.py`。
#### 步驟 / 輸入輸出
```bash
cd C:\Programming\ExampleEnv\UUUGPT\
python upload.py
```
#### 圖表 / 表格描述
- 本頁較像效能比較圖而非流程圖；若還原成折線或長條圖，橫軸可視為 `CPU threads / GPU`，縱軸為 `處理時間`。
- 趨勢方向為：`thread 數增加` -> `CPU 處理時間下降`；另有一條 `GPU 3090` 路徑，在更大資料量下仍維持較低耗時。
- 圖想表達的關係不是單點數字，而是「平行度與 GPU 都能明顯改善 CKIP 批次處理吞吐量」。

## RAG、微調、多模態與 Agent

### Page 61 — 開始和 pdf 文件聊天
#### 主題
- 與 PDF 文件交互聊天。
#### 重點
- 使用 Local RAG Server 與 PDF 對話。
- 建議同時查看 `upload.py` 與 `chat.py`。
#### 步驟 / 輸入輸出
```bash
python chat.py
```
- 輸入：上傳的 PDF。
- 輸出：依 PDF 內容回應的聊天結果。
#### 圖表 / 表格描述
- 可還原為 LangGraph 狀態圖：`使用者請求` 進入 `路由節點(route_tools)`，再依判斷分流到 `順序節點` 或 `並行節點`。
- 工具執行節點可視為 `BasicToolNode`，其輸出會回傳到 `LLM 決策節點`，最後再流向 `最終回答`。
- 若原圖有分叉箭頭，重點應是 `一個輸入` 可依任務類型分派到 `多個工具或多條子流程`，而不是固定單線流程。

### Page 62 — Lesson 35 - Google Gemini-Pro + LlamaIndex + RAG
#### 主題
- Gemini-Pro 結合 LlamaIndex 做 RAG。
#### 重點
- 文件分割、Embedding、多語系向量、與文件聊天。
- 進階主題：Multi Chain、Source Tracing、Conversational QA。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1LyNYnGMBxNQrXEXuYeDL_OTkqRiT1PM4?usp=sharing`
#### 圖表 / 表格描述
- 可重建為最小 MCP 架構圖：`FastMCP Server` 內含兩個工具節點 `get_time()` 與 `get_date_info()`。
- 執行流程為：`conda activate uuugpt` -> `安裝 fastmcp 與 web server 依賴` -> `python mcp_server.py` 啟動服務。
- 對外箭頭可描述為：`Client / LM Studio` -> `http://localhost:8000` -> `工具函式` -> `時間 / 日期字串`。

### Page 63 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 可還原為客戶端互動流程：`mcp_client.py` -> `連線 MCP server` -> `列出工具清單` -> `呼叫 get_time / get_date_info` -> `回傳字串結果`。
- 若原圖含請求與回應方向，箭頭應是雙向：`Client` 向伺服器發送 request，`Server` 再回傳工具執行結果。
- 圖的重點不是 UI，而是說明 MCP client 與 server 的呼叫關係已成立。

### Page 64 — Lesson 40 - LlamaIndex Splitter 分片資料的比較
#### 主題
- 比較不同 Splitter 的分片結果。
#### 重點
- 測試不同 splitter 對分片 node 的影響。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1LbH_Lf-LseuNWdFZTyFN6SfDjgLVgYm0?usp=sharing`
#### 圖表 / 表格描述
- 可重建為 LM Studio 設定流程：`開啟 LM Studio` -> `Program / 設定頁` -> `編輯 mcp.json` -> `加入 MyTimeServer url` -> `儲存設定`。
- 設定資料流方向為：`LM Studio 設定檔` -> `mcpServers` -> `MyTimeServer` -> `http://localhost:8000`。
- 若原圖有畫面框線，核心語意是「把外部 MCP server 註冊進 LM Studio 的可用工具清單」。

### Page 65 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 可視為驗證流程圖：`LM Studio 啟動` -> `讀取 mcp.json` -> `發現 MyTimeServer` -> `連線成功` -> `工具顯示為可啟用`。
- 箭頭關係強調註冊成功後，MCP service 會出現在 LM Studio 的服務或工具列表中，並可切換啟用狀態。

### Page 66 — Lesson 24 - Fine-tuning ChatGPT 從問答集產生客服機器人
#### 主題
- 使用 OpenAI Fine-tuning 建立客服機器人。
#### 重點
- 建立 API key、上傳訓練資料、建立 fine-tune job、監控、測試、刪除模型。
- 訓練資料集為 QA JSONL 對話格式。
#### 步驟 / 輸入輸出
- 資料集：`https://huggingface.co/datasets/DavidLanz/fine_tuning_datraset_4_openai`
- 來源程式碼：`https://colab.research.google.com/drive/1MMQhE3XTmV7bYNZBpWcG3OXfhyRkfGDv?usp=sharing`
#### 圖表 / 表格描述
- 可還原為多 GPU serving 架構：`docker-compose` 啟動 `vllm-openai server`，server 內部再把模型權重切到 `4 張 GPU` 做 tensor parallel。
- 主要資料流方向為：`外部 client` -> `vLLM OpenAI-compatible API` -> `多 GPU 推理` -> `文字生成結果`。
- 環境變數與 `gpu-memory-utilization`、`max-model-len` 屬於 server 節點的設定屬性，而非獨立流程節點。

### Page 67 — Lesson 25.4 - Finetune LLaMA 3.2 3B with Chinese instruction datasets
#### 主題
- 微調 Llama 3.2 3B，合併 QLoRA adapter 並輸出 GGUF。
#### 重點
- 先在 HuggingFace 建立 `llama3.2_3B_news_qlora` 與 `llama3.2_3B_news_merged`。
- 有 Colab Pro 才建議在雲端轉檔。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/12kj4G6qy-jq58GKYcprWQ4BmkV33k6xq?usp=sharing`
#### 圖表 / 表格描述
- 可重建為 client-server 連線圖：`client 容器` 透過 `VLLM_BASE_URL=http://host.docker.internal:8000/v1` 指向 `vLLM server`。
- 依賴套件節點位於 client 端，作用是完成前處理、向量比對或批次呼叫；箭頭則指向遠端推理服務。
- 圖說重點是「推理服務與業務邏輯容器分離」。

### Page 68 — Lesson 30 - 使用 OpenAI 的 Whisper 模型建立 YouTube 字幕
#### 主題
- 以 Whisper 將 YouTube 影片轉成字幕。
#### 重點
- 選擇 Whisper 模型。
- 輸入影片網址，輸出字幕與 `youtube.srt`。
#### 步驟 / 輸入輸出
- 來源程式碼：
  - `https://colab.research.google.com/drive/10dx_ZI3dYjvXMV4bc_U4To60yKEcaeNR?usp=sharing`
  - `https://colab.research.google.com/drive/10_xLYzCltgSXCfW7-6417U2ciEuj4xlK?usp=sharing`
#### 圖表 / 表格描述
- 可還原為批次推論流程：`輸入資料夾` -> `compare_cli_vllm.py` -> `依 BATCH_SIZE 分批送出請求` -> `vLLM server` -> `輸出結果資料夾`。
- 箭頭方向清楚表達了資料由本地批次腳本流向遠端推理 API，再回寫到輸出目錄。

### Page 69 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁可視為可靠性流程：`容器啟動` -> `health check` -> `curl /v1/models` -> `成功則維持服務`，失敗則走 `restart on-failure` 路徑。
- 若圖中含 volume 或 cache，應屬附屬節點，連到主服務節點以保留模型或中間資料。

### Page 70 — Lesson 31 - 使用 OpenAI 產出 YouTube 字幕與 Assistant 助手
#### 主題
- 將字幕轉成 OpenAI Assistant 可用素材。
#### 重點
- 建立 Assistant、上傳字幕 `.txt`、建立聊天主題。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1Ngteuo7nNqxeXhHqgmfIJ2m7EqFJwLCA?usp=sharing`
#### 圖表 / 表格描述
- 可重建為壓測回路：`stress_vllm.sh` -> `設定併發數 / prompt / token 數` -> `大量發送請求到 vLLM` -> `收集總耗時與 RPS`。
- 箭頭方向代表 `測試腳本` 持續向 `vLLM API` 送流量，再回收 `吞吐量與延遲` 指標。
- 圖的重點是效能驗證，不是部署本身。

### Page 71 — YouTube 字幕檔案助手
#### 主題
- 在 ChatGPT 中分析字幕檔並帶出 timestamp URL。
#### 重點
- 下載 `Vm5LB6Rlkmg.txt` 上傳到 ChatGPT。
- 用中文回答，並附 YouTube timestamp 連結。
#### 步驟 / 輸入輸出
- 下載：`https://tinyurl.com/26kcn5xd`
- 問題範例：擷取 VideoId 與帶 timestamp 的 YouTube URL。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 72 — LAB : 使用 FLUX 模型文生圖
#### 主題
- 使用 FLUX 生成圖像。
#### 重點
- 網址：`https://huggingface.co/spaces/vilarin/lumiere`
- 提供女生版提示詞。
#### 步驟 / 輸入輸出
- 輸入：文生圖 prompt。
- 輸出：生成圖片。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 73 — 重新整理網頁，輸入男生版提示詞
#### 主題
- 使用 FLUX 生成男生版圖像。
#### 重點
- 使用長 prompt 指定 East Asian mixed race、café、雨天、Beer 牌子等細節。
#### 步驟 / 輸入輸出
- 輸入：男生版 prompt。
- 輸出：對應生成圖。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 74 — LAB : 使用 Z-Image-Turbo 模型文生圖
#### 主題
- 使用 Z-Image-Turbo 做女生版圖像生成。
#### 重點
- 網址：`https://huggingface.co/spaces/mrfakename/Z-Image-Turbo`
#### 步驟 / 輸入輸出
- 輸入：女生版 prompt。
- 輸出：生成圖像。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 75 — 輸入男生版提示詞
#### 主題
- 使用 Z-Image-Turbo 生成男生版圖像。
#### 重點
- prompt 與 FLUX 類似，但在另一個空間執行。
#### 步驟 / 輸入輸出
- 輸入：男生版 prompt。
- 輸出：圖像。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 76 — 多模態模型 看圖說故事
#### 主題
- 利用多模態模型做圖片理解與故事生成。
#### 重點
- 開啟 Lesson 43 的短網址。
- 先在雲端硬碟儲存副本。
#### 步驟 / 輸入輸出
- 輸入：圖像。
- 輸出：敘事式故事或理解結果。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 77 — Lesson 36 - Convert Model to GGUF format
#### 主題
- 將模型轉換為 GGUF 與量化格式比較。
#### 重點
- Q2_K、Q3_K、Q4_K、Q5_K、Q6_K、Q8_K 的 bpw 與區塊結構。
- 說明 type-0 與 type-1 的差異。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1K7XfGNd8_FLJl2cB7Uvoam3gQZG7yC1t?usp=sharing`
#### 圖表 / 表格描述
| 類型 | 說明 |
|---|---|
| Q2_K | 2.5625 bpw |
| Q3_K | 3.4375 bpw |
| Q4_K | 4.5 bpw |
| Q5_K | 5.5 bpw |
| Q6_K | 6.5625 bpw |
| Q8_K | 中間結果量化 |

### Page 78 — Bonus round : Create GPTs on OpenAI
#### 主題
- 建立自訂 GPT 與 URL action。
#### 重點
- 範例 GPT：LnData 美妝產業知識家。
- 要求可讀網址內容、總結新聞、分析觀點、翻譯成繁中。
#### 步驟 / 輸入輸出
1. New GPTs -> Create。
2. 建立 Action。
3. 使用 AWS Lambda function 提供 API。
4. 關閉瀏覽器 checkbox。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 79 — parameters:
#### 主題
- OpenAPI 參數定義片段。
#### 重點
- `url` 參數型別為 string，來源於 query，必填。
#### 步驟 / 輸入輸出
```yaml
parameters:
  - name: url
    schema:
      type: string
    in: query
    required: true
    description: URL of a news article
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 80 — Gemini Gem
#### 主題
- Google Gemini 社群文案助手。
#### 重點
- 可做貼文撰寫、標題與 hashtag、懶人包、主題規劃、語氣調整、輔助建議。
#### 步驟 / 輸入輸出
- 輸入：產品、品牌、受眾、語氣、平台。
- 輸出：多版本貼文內容與腳本建議。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 81 — 語氣調整
#### 主題
- 文案助手的輸出策略。
#### 重點
- 可依溫暖、促銷、正式、KOL、品牌感調整。
- 若資訊不足，主動索取背景資料。
#### 步驟 / 輸入輸出
- 輸入：語氣偏好。
- 輸出：調整後文案。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 82 — 使用大衛的社群文案助手 Gem
#### 主題
- 以 YSL 唇膏為案例產生社群貼文。
#### 重點
- 產品：YSL Pur Couture The Bold #02。
- 平台：IG、小紅書。
- 產出 3 篇貼文，附標題、內文、Hashtag、圖片或 Reels 構想。
#### 步驟 / 輸入輸出
- 輸入：產品名稱、特色、受眾、平台、語氣。
- 輸出：3 篇文案。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 83 — Day break;
#### 主題
- 日間休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 84 — Lesson 50.1 AI Agent and Tools with LangChain
#### 主題
- 以 LangChain 建立 AI Agent 與 Tools。
#### 重點
- 安裝套件、下載 Ollama / Llama 3.2 3B。
- 新增 tool、解析多變數、`create_react_agent`、`create_tool_calling_agent`、MemorySaver。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1cKgKLMMXV2FiXeGJfDYYuH-eCjZA2lIx?usp=sharing`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 85 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 86 — Lesson 50.2 AI Agent and Tools with LangGraph
#### 主題
- 以 LangGraph 建立具路由與並行能力的 Agent。
#### 重點
- 順序節點、並行節點、route_tools、BasicToolNode、Ollama + Llama 3.2 3B、讓 LLM 自主尋找適合工具。
#### 步驟 / 輸入輸出
- Colab：`https://colab.research.google.com/drive/1leMgX3oyTJZ7FjNnb_RNA-quQVJljwdL?usp=sharing`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 87 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 88 — Lesson 58: 使用 LLM 將非結構化資料轉換為知識圖譜
#### 主題
- 使用 LLM 萃取 SPO 並建立知識圖譜。
#### 重點
- 從文字分塊到 SPO 抽取、正規化、去重、NetworkX、Neo4j 匯入與 Cypher 查詢。
#### 步驟 / 輸入輸出
- 來源程式碼：`https://colab.research.google.com/drive/1gWqlSQOcfo4p9FGcSSTisqutt08gewO8?usp=sharing`
- Neo4j：`https://neo4j.com/product/auradb/`
#### 圖表 / 表格描述
- 可視為流程：文本分塊 -> SPO 抽取 -> 去重 -> 建圖 -> 匯入 Neo4j -> Cypher 問答。

### Page 89 — Lesson 58.7: 將非結構化資料轉換為知識圖譜_Embedding
#### 主題
- 在知識圖譜上加入 Embedding 與語意檢索。
#### 重點
- 除了前一頁流程外，新增 Embedding、寫入 Neo4j、建立向量索引、語意檢索 + LLM 問答。
#### 步驟 / 輸入輸出
- 來源程式碼：`https://colab.research.google.com/drive/13TDut-GN0F1pD3jZg5q8NzUpbx4ZC6yg?usp=sharing`
#### 圖表 / 表格描述
- 可視為流程：原知識圖譜 -> 向量化 -> 向量索引 -> 語意檢索 -> 問答。

### Page 90 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

## 本地部署、MCP、vLLM 與課程收尾

### Page 91 — Deploy LLM model with your own PC
#### 主題
- 在個人電腦本地部署 LLM。
#### 重點
- 常見工具：LM Studio、Text Generation WebUI、GPT4All、Jan、llama.cpp、ollama、Anything LLM。
- 本章以 LM Studio 為主。
#### 步驟 / 輸入輸出
1. 造訪 `https://lmstudio.ai/` 下載對應系統版本。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 92 — LM Studio 安裝步驟
#### 主題
- 安裝 LM Studio。
#### 重點
- 雙擊執行檔即可安裝。
#### 步驟 / 輸入輸出
- 步驟 2：雙擊執行安裝程式。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 93 — GGUF 模型檔案放置位置
#### 主題
- LM Studio 模型目錄與自訂模型資料夾。
#### 重點
- Windows 與 macOS 預設路徑不同。
- 也可自行建立 `D:\Programming\model\` 等自訂結構。
#### 步驟 / 輸入輸出
- Windows：`C:\Users\你的UserId\.lm-studio\models\...`
- MacOS：`/Users/你的UserId/.lm-studio/models/...`
#### 圖表 / 表格描述
| 系統 | 預設路徑 |
|---|---|
| Windows | `C:\Users\<UserId>\.lm-studio\models\` |
| macOS | `/Users/<UserId>/.lm-studio/models/` |

### Page 94 — LM Studio 首次啟動
#### 主題
- 首次啟動導引。
#### 重點
- 點擊紫色 `Get Started`。
#### 步驟 / 輸入輸出
- 輸入：啟動程式。
- 輸出：安裝導引介面。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 95 — LM Studio 模型下載導引
#### 主題
- 跳過自動下載模型。
#### 重點
- 課堂中不要在 LM Studio 內直接下載模型。
- 講師會提供 GGUF 模型資料夾。
#### 步驟 / 輸入輸出
- 點擊 `Skip for now`。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 96 — 進入 uuugpt 虛擬環境，開啟 jupyter notebook
#### 主題
- 進入 `uuugpt` 環境並啟動 Jupyter。
#### 重點
- 在 `C:\Programming\ExampleEnv` 下啟動 notebook。
#### 步驟 / 輸入輸出
```bash
conda activate uuugpt
cd C:\Programming\ExampleEnv
jupyter notebook
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 97 — LAB：Chat with your model (GGUF) via LM Studio
#### 主題
- 透過 OpenAI 相容 API 呼叫本地 GGUF 模型。
#### 重點
- `base_url` 使用 `http://localhost:1234/v1`。
- system prompt 可指定模型角色，如台灣美食專家。
- 支援串流輸出與對話歷史。
#### 步驟 / 輸入輸出
```python
from openai import OpenAI
client = OpenAI(base_url="http://localhost:1234/v1", api_key="lm-studio")
```
- 輸入：使用者提問。
- 輸出：模型串流回應。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 98 — HTML Chatbot 與 Local API
#### 主題
- 用 Flask + LangChain + LM Studio 建立本地聊天 API。
#### 重點
- 建立 `Chat/app.py`。
- 啟用 CORS，將用戶訊息轉為 LangChain `HumanMessage`。
#### 步驟 / 輸入輸出
```bash
conda activate uuugpt
mkdir C:\Programming\ExampleEnv\Chat
cd C:\Programming\ExampleEnv\Chat
python app.py
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 99 — Flask API 錯誤處理實現
#### 主題
- 串流生成與錯誤處理。
#### 重點
- `llm.stream(chat_history)` 逐段回傳。
- 例外時回傳 `[ERROR]` 字串。
#### 步驟 / 輸入輸出
```python
def generate():
    try:
        for chunk in llm.stream(chat_history):
            if chunk.content:
                yield chunk.content
    except Exception as e:
        yield f"\n[ERROR] {str(e)}"
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 100 — HTML 聊天機器人介面建立
#### 主題
- 建立 `chat.html` 與前端樣式。
#### 重點
- 聊天窗固定於右下角，訊息區可滾動。
#### 步驟 / 輸入輸出
- 建立位置：`C:\Programming\ExampleEnv\Chat\chat.html` 或 `C:\Apache24\htdocs\chat.html`。
- 設定 API URL 與 DOM 結構。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 101 — HTML 輸入框與發送按鈕設定
#### 主題
- 前端輸入區與按鈕樣式。
#### 重點
- 使用 flex 排版。
- 發送按鈕為綠色主色。
#### 步驟 / 輸入輸出
```css
#input { flex: 1; padding: 10px; border: none; outline: none; }
#send  { padding: 10px; background: #4CAF50; color: white; border: none; cursor: pointer; }
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 102 — JavaScript 聊天邏輯實現
#### 主題
- 前端聊天邏輯與串流處理。
#### 重點
- 驗證空輸入。
- 使用 `fetch` 與後端 API 通信。
- 以 `TextDecoder` 解碼串流回應。
#### 步驟 / 輸入輸出
```javascript
if (!userInput) return;
const response = await fetch(API_URL, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ messages: chatHistory }),
});
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 103 — LM Studio RAG 功能
#### 主題
- 將 PDF 文件上傳至 LM Studio 進行 RAG 問答。
#### 重點
- 範例文件：`Se_C_04.pdf`。
- 設定系統提示詞，強化分析能力與正體中文回覆。
#### 步驟 / 輸入輸出
1. 下載：`https://www.ios.sinica.edu.tw/people/personal/ctang/Se_C_04.pdf`
2. 上傳到 LM Studio Chat。
3. 設定系統提示詞。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 104 — LM Studio RAG 查詢範例
#### 主題
- 基於文件內容提問。
#### 重點
- 問題範例：台灣人如何理解運動，與西方 sport 有何差異。
#### 步驟 / 輸入輸出
- 輸入：問題。
- 輸出：結合 PDF 內容的回答。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 105 — MCP Server 取得目前時間工具
#### 主題
- 以 FastMCP 建立時間工具伺服器。
#### 重點
- 建立 `mcp_server.py`，提供 `get_time()` 與 `get_date_info()`。
- 時區為 `Asia/Taipei`。
#### 步驟 / 輸入輸出
```bash
mkdir mcp
conda activate uuugpt
pip uninstall mcp
pip install fastapi uvicorn
pip install --upgrade fastmcp
python mcp_server.py
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 106 — MCP Client 實現
#### 主題
- 建立 MCP client 呼叫遠端工具。
#### 重點
- 連線到 `http://127.0.0.1:8000/`。
- 列出工具並呼叫 `get_time`、`get_date_info`。
#### 步驟 / 輸入輸出
- 輸入：`mcp_client.py`。
- 輸出：工具清單與時間字串。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 107 — 新增 MCP Server 至 LM Studio
#### 主題
- 在 LM Studio 中註冊自訂 MCP Server。
#### 重點
- LM Studio 需版本 >= 0.3.17。
- 在 Program 標籤中編輯 `mcp.json`。
#### 步驟 / 輸入輸出
```json
{
  "mcpServers": {
    "MyTimeServer": {
      "url": "http://localhost:8000"
    }
  }
}
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 108 — MCP Server 新增成功確認
#### 主題
- 驗證 LM Studio 已成功連上 MCP 服務。
#### 重點
- 啟用自己的 MCP service。
#### 步驟 / 輸入輸出
- 在設定中確認新增成功，再啟用服務。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 109 — MCP Tool 測試呼叫
#### 主題
- 直接在聊天框測試工具呼叫。
#### 重點
- 使用 `@MyTimeServer.get_time()`。
#### 步驟 / 輸入輸出
- 輸入：`@MyTimeServer.get_time()`。
- 輸出：工具呼叫結果。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 110 — MCP 時間查詢測試
#### 主題
- 使用自然語言讓模型自動觸發時間工具。
#### 重點
- 範例問題：`現在的時間是?`
#### 步驟 / 輸入輸出
- 輸入：自然語言問題。
- 輸出：模型應自動調用 `get_time()` 並回覆結果。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 111 — break;
#### 主題
- 課程休息標記。
#### 重點
- 無。
#### 步驟 / 輸入輸出
- 無。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 112 — 在伺服器上部署多張 NVIDIA 顯示卡
#### 主題
- 用 Docker 與 vLLM 進行多 GPU 分散式推理部署。
#### 重點
- 使用 `vllm/vllm-openai:v0.11.0`。
- `tensor-parallel-size: 4`、`gpu-memory-utilization: 0.92`、`max-model-len: 8192`。
#### 步驟 / 輸入輸出
- 伺服器端 `docker-compose.yml` 配置多 GPU 與 NCCL 環境變數。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 113 — 客戶端套件與配置
#### 主題
- 客戶端依賴與 docker-compose 配置。
#### 重點
- 套件：langchain、numpy、scikit-learn、tqdm、PyYAML、sentence-transformers、aiohttp。
- 透過 `VLLM_BASE_URL` 指向 `http://host.docker.internal:8000/v1`。
#### 步驟 / 輸入輸出
- 輸入：客戶端配置與 requirements。
- 輸出：可對 vLLM 伺服器送請求的 client 容器。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 114 — 批次大小與容器啟動配置
#### 主題
- 客戶端容器的 batch size 與啟動命令。
#### 重點
- `BATCH_SIZE` 可依記憶體調整 32/64/128。
- 啟動時會啟用進度列並執行 `compare_cli_vllm.py`。
#### 步驟 / 輸入輸出
```bash
python /app/compare_cli_vllm.py -i /app/data -m sentence-transformers/distiluse-base-multilingual-cased-v1 -o /app/output -t 0.75 -p 10
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 115 — 健康檢查與重啟策略
#### 主題
- 容器健康檢查、重啟與快取 volume。
#### 重點
- 每 10 秒檢查一次模型 API 是否可用。
- `retries: 12`。
- `restart: on-failure`。
#### 步驟 / 輸入輸出
- 健康檢查會用 `curl -sf http://host.docker.internal:8000/v1/models`。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 116 — 壓測 stress_vllm.sh
#### 主題
- 壓力測試 vLLM 服務。
#### 重點
- 可調整併發數、token 數、prompt。
- 計算總耗時與 RPS。
#### 步驟 / 輸入輸出
```bash
./stress_vllm.sh
./stress_vllm.sh 16
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 117 — 即時檢查顯示卡使用率
#### 主題
- GPU 即時監控。
#### 重點
- 使用 `watch -n 1 nvidia-smi`。
#### 步驟 / 輸入輸出
```bash
watch -n 1 nvidia-smi
```
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 118 — Python 中文 NLP 與 LLM 課程意見調查
#### 主題
- 問卷填寫與教學回饋。
#### 重點
- 問卷網址：`https://survey.uuu.com.tw`
- 班別：`26832`
#### 步驟 / 輸入輸出
1. 使用恆逸教室電腦或 Lab 電腦連線。
2. 輸入班別 `26832`。
3. 選擇科目明細中的本次課程。
4. 完成後按儲存資料。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 119 — 課程範例程式下載與講師資訊
#### 主題
- 課程資源與講師經歷總覽。
#### 重點
- AllExample 下載：`https://www.dropbox.com/s/ffixfp7gb59c8ij/AllExample.rar?dl=0`，密碼 `uuu`。
- 列出講師在恆逸的其他課程與國際技能競賽培訓紀錄。
#### 步驟 / 輸入輸出
- 輸入：下載並解壓 AllExample。
- 輸出：所有範例程式。
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。

### Page 120 — 講師證照與著作
#### 主題
- David Lanz 的專業認證與出版作品。
#### 重點
- AWS、Google Cloud、Microsoft、Sun Microsystems、PHP Zend 認證。
- 多本技術書籍，涵蓋 Android、PHP、C#、Java 等。
#### 步驟 / 輸入輸出
- 本頁主要是清單型資訊。
#### 圖表 / 表格描述
| 類型 | 內容 |
|---|---|
| 證照 | AWS、GCP、Microsoft、Sun、Zend |
| 著作 | Android、PHP、C#、Java 等書籍 |

### Page 121 — 講師專利與聯絡資訊
#### 主題
- 專利、聯絡方式與課程結語。
#### 重點
- 三項專利：QR 碼交易、意見領袖交易管理、資料交換方法。
- 聯絡信箱：`DavidLanz@gmail.com`。
#### 步驟 / 輸入輸出
- 本頁主要是專利清單與結尾訊息：`See you soon! Farewell & happy coding!`
#### 圖表 / 表格描述
- 本頁原始轉寫未保留可描述的圖表細節。
